{% extends 'baseAdmin.html.twig' %}

{% block title %}Créer une Image{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Créer une Image</h1>

    {{ include('image/_form.html.twig') }}

        <button class="btn btn-primary"><a href="{{ path('image_index') }}">Retour à la Liste</a></button>s
    </div>
{% endblock %}
