{% extends 'baseAdmin.html.twig' %}

{% block title %}Editer une Image{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Editer une Image</h1>

    {{ include('image/_form.html.twig', {'button_label': 'Modifier'}) }}

        <button class="btn btn-primary"><a href="{{ path('image_index') }}">Retour à la Liste</a></button>

    {{ include('image/_delete_form.html.twig') }}
    </div>
{% endblock %}
