{% extends 'baseAdmin.html.twig' %}

{% block title %}Créer un About{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Créer un About</h1>

    {{ include('about/_form.html.twig') }}

        <button class="btn btn-primary"><a href="{{ path('about_index') }}">Retour à la Liste</a></button>
    </div>
{% endblock %}
