{% extends 'baseAdmin.html.twig' %}

{% block title %}Editer un About{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Editer un About</h1>

    {{ include('about/_form.html.twig', {'button_label': 'Modifier'}) }}

        <button class="btn btn-primary"><a href="{{ path('about_index') }}">Retour à la Liste</a></button>

    {{ include('about/_delete_form.html.twig') }}
    </div>
{% endblock %}
