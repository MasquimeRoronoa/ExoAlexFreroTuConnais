<form method="post" action="{{ path('calendar_delete', {'id': calendrier.id}) }}" onsubmit="return confirm('Supprimer cet Evenement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendrier.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
