{% extends 'baseAdmin.html.twig' %}

{% block title %}Editer un Features{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Editer un Features</h1>

    {{ include('features/_form.html.twig', {'button_label': 'Modifier'}) }}

        <button class="btn btn-primary"><a href="{{ path('features_index') }}">Retour à la Liste</a></button>

    {{ include('features/_delete_form.html.twig') }}
    </div>
{% endblock %}
