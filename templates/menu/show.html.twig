{% extends 'baseAdmin.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ menu.NomMenu }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ menu.DescMenu }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td><i class="{{ menu.IconMenu }}"></i></td>
            </tr>
        </tbody>
    </table>

        <button class="btn btn-primary"><a href="{{ path('menu_index') }}">Retour à la Liste</a></button>

            <button class="btn-danger"><a href="{{ path('menu_edit', {'id': menu.id}) }}">Editer</a></button>

    {{ include('menu/_delete_form.html.twig') }}
    </div>
{% endblock %}
