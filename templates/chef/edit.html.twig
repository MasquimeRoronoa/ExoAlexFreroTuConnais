{% extends 'baseAdmin.html.twig' %}

{% block title %}Editer un Chef{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Editer un Chef</h1>

    {{ include('chef/_form.html.twig', {'button_label': 'Modifier'}) }}

        <button class="btn btn-primary"><a href="{{ path('chef_index') }}">Retour à la Liste</a></button>

    {{ include('chef/_delete_form.html.twig') }}
    </div>
{% endblock %}
