<form method="post" action="{{ path('chef_delete', {'id': chef.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce Chef ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chef.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
