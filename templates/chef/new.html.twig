{% extends 'baseAdmin.html.twig' %}

{% block title %}Créer un Chef{% endblock %}

{% block body %}
    <div class="content-wrapper">
    <h1>Créer un Chef</h1>

    {{ include('chef/_form.html.twig') }}

    <button class="btn btn-primary"><a href="{{ path('chef_index') }}">Retour à la Liste</a></button>
    </div>
{% endblock %}
